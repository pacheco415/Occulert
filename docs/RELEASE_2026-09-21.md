# Release progress — September 21, 2026 UTC

## Fleet follow-ups

PR #125 was merged at `1113d72506ffb4044325d21b583c898118a15e2f`. Vercel production deployment `dpl_2VD9r2J5wiEKUMDWZvjn1kDECj2q` reached READY. The production dashboard includes the new asset, whose SHA-256 matches the tested source. Unauthenticated requests to the follow-up API return 401.

The existing Preview and production environments both use Supabase project `wbsynfcjpwlgdzioqpoa`; no separate hosted staging database was available. The approved `fleet_session_followups` migration was applied there. A transaction using synthetic records verified server-role creation/update, stale-revision conflicts, cross-owner denial, client access restrictions, and RLS. All synthetic records were rolled back. Prior browser checks passed against simulated authenticated responses; a real signed-in browser save/reload remains a follow-up check.

The production error-log query found no matching error/fatal entries during the immediate release window. This is a short observation window, not continuous monitoring.

## Native readiness

PR #124 was merged at `699583cb6ad80bfdd7bf969c8743acaba3ef8a4a`. Site audit, native typecheck, and browser workflows passed. This does not publish an iPhone binary. Expo CLI reports `Not logged in` on this Mac, so no new signed build or TestFlight submission has been made. Physical iPhone, Watch, and headphone acceptance checks remain in `PARKED_DEVICE_READINESS.md`.

## Existing advisory findings

The security advisor reported an existing [leaked-password protection warning](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection). Server-only tables have RLS enabled without client policies by design; do not add public policies merely to silence the informational notice. Performance advice also identifies opportunities to reduce repeated auth checks inside older RLS policies. These settings and policies are not changed by the follow-up release or index-only improvement.
